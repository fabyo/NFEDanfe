name: Scorecard

on:
  schedule:
    - cron: '30 1 * * 6'
  push:
    branches: [ main ]

permissions:
  security-events: write
  id-token: write
  contents: read

jobs:
  analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: ossf/scorecard-action@v2
